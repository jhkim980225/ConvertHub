/**
 * 이미지를 PDF로 변환하는 툴
 */

import { ImagesToPDFConverter } from '@/components/image/ImagesToPDFConverter';

/**
 * 이미지 → PDF 변환 툴
 */
export const ImageToPDFTool: React.FC = () => {
  return <ImagesToPDFConverter />;
};

